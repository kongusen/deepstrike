{
  "expect": "missing_field",
  "note": "DEC-8 · the host effect support declaration is mandatory; defaulting it re-introduces the implicit assumption it exists to remove",
  "envelope": {
    "operation_id": "op-2026-07-29-0001",
    "input_id": "cfg-reject-4",
    "observed_at_ms": "1753747200000",
    "input": {
      "kind": "configure_operation",
      "config": { "execution_policy": { "max_turns": 10 } }
    }
  }
}
