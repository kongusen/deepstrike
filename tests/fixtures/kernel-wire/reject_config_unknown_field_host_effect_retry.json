{
  "expect": "unknown_field",
  "note": "§13.3 · host_effect_retry_attempts is host executor policy, not kernel config",
  "envelope": {
    "operation_id": "op-2026-07-29-0001",
    "input_id": "cfg-reject-3",
    "observed_at_ms": "1753747200000",
    "input": {
      "kind": "configure_operation",
      "config": { "recovery_policy": { "provider_recovery_attempts": 2, "host_effect_retry_attempts": 3 }, "host_effect_support": { "supported": ["call_provider"] } }
    }
  }
}
