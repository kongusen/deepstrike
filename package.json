{
  "name": "deepstrike-workspace",
  "version": "0.2.60",
  "private": true,
  "description": "Agent OS microkernel for cross-language agent runtimes - workspace toolchain",
  "scripts": {
    "test:conformance": "node scripts/run-sdk-conformance.mjs",
    "test:conformance:runner": "node --test scripts/run-sdk-conformance.test.mjs",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "docs:drift": "node scripts/check-docs-drift.mjs",
    "docs:manifest": "node scripts/extract-symbol-manifest.mjs",
    "contracts:check": "npm run contracts:generate && node scripts/check-semantic-contracts.mjs",
    "contracts:generate": "node scripts/generate-contract-projections.mjs"
  },
  "devDependencies": {
    "vitepress": "^1.5.0",
    "vue": "^3.4.21"
  }
}
