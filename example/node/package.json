{
  "name": "flashnote-demo",
  "version": "0.1.0",
  "description": "FlashNote — 闪念整理助手 demo built on @deepstrike/sdk",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "NODE_OPTIONS=--no-deprecation tsx src/server.ts",
    "cli": "NODE_OPTIONS=--no-deprecation tsx src/main.ts",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@deepstrike/sdk": "^0.1.3",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.0.0"
  }
}
