# Unified Public Agent Model Implementation Plan

The Node SDK is being replaced with an executable Agent facade. Ordinary applications configure an Agent and call `run`, `stream`, `session`, memory, delegation, or workflow methods. Kernel, journal, harness, and runtime implementation details move to the advanced subpath.

The detailed task sequence is maintained in `docs/plans/unified-public-agent-model.md`.
